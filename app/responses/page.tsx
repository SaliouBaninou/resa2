import EntreprisePage from "@/app/dashboard/entreprises/page";

export default function ResponsesPublicPage() {
  // Render the same UI as the entreprises listing at /dashboard/entreprises
  return <EntreprisePage />;
}
